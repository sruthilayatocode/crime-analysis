# Backend

## Purpose
Web application server that handles HTTP requests, API routing, business logic, and database interactions.

## Files
- `app.py` — Application entry point and configuration
- `routes.py` — API endpoint definitions
- `models.py` — Database ORM models
- `__init__.py` — Package initialization

## Future Work
- Implement RESTful API endpoints for crime data CRUD operations
- Add authentication and authorization middleware
- Integrate with ML prediction service
- Add WebSocket support for real-time alerts